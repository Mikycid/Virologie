export interface User {
    uuid: string
    ip: string
    port: number
    connection_time: string
    is_admin: boolean
    logged: boolean
    username: string
    last_name: string
    first_name: string
}